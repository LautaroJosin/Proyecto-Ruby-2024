<div class="container mt-4">
  <h1 class="text-center text-primary mb-4">Users List</h1>

  <p class="mb-4">Users count: <%= @users.count %></p>

  <% if @users.count > 0 %>
    <table class="table table-striped table-bordered">
      <thead class="table-dark">
        <tr>
          <th>Email</th>
          <th>Username</th>
          <th>Phone</th>
          <th>Role</th>
          <th>Active</th>
          <th>Actions</th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.email %></td>
            <td><%= user.username %></td>
            <td><%= user.phone %></td>
            <td><%= user.role_int %></td>
            <td>
                <% if user.is_active %>
                    <span>Yes</span>
                <% else %>
                    <span>No</span>
                <% end %>
            </td>
            <td>
            <div class="d-flex">
              <% if user.is_active %>
                <%= button_to 'Deactivate', deactivate_user_path(user), method: :patch, class: 'btn btn-danger btn-sm me-2' %>
              <% else %>
                <%= button_to 'Activate', '#', method: :patch, class: 'btn btn-success btn-sm me-2' %>
              <% end %>
              <%= link_to 'Edit', edit_user_path(user), class: 'btn btn-primary btn-sm' %>
            </div>
            </td>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="text-center text-danger">There are no users to show</p>
  <% end %>
</div>